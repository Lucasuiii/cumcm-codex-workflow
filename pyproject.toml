[project]
name = "cumcm-codex-workflow"
version = "0.6.0"
description = "Contest-native, evidence-focused workflow for CUMCM"
license = {file = "LICENSE"}
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.18,<5",
  "referencing>=0.35,<1",
]

[tool.unittest]
start-directory = "tests"
