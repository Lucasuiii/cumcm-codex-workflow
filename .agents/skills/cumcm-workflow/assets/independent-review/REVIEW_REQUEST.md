# 独立复核请求

请在不读取原建模对话的条件下，独立核对本包中的官方问题、模型、计算程序、运行记录和结果。

重点确认：

1. 模型是否回答了题目真正要求的问题；
2. 目标方向、上下界、单位、约束和物理含义是否一致；
3. 是否存在重复计量、遗漏变量、跨问矛盾或无依据外推；
4. 代码是否实现了文档声明的模型；
5. 结果是否来自保存的正式运行，并满足官方约束；
6. 哪些结论可以进入 validation，哪些必须缩窄、补算或撤回。

不要根据成功退出码、文件存在或原作者自评直接放行。每个问题都要给出可定位的证据。
