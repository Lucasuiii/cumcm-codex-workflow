[project]
name = "cumcm-codex-workflow"
version = "0.3.0"
description = "Codex-native reproducible workflow for CUMCM"
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.18"]

[tool.unittest]
start-directory = "tests"
